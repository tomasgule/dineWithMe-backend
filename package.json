{
  "name": "dineWithMe",
  "version": "1.0.0",
  "scripts": {
    "run": "pipenv run python3 manage.py runserver",
    "migrate": "pipenv run python3 manage.py migrate",
    "makemigrations": "pipenv run python3 manage.py makemigrations",
    "install": "pipenv install"
  }
}
